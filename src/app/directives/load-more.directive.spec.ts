import { LoadMoreDirective } from './load-more.directive';

xdescribe('LoadMoreDirective', () => {
  it('should create an instance', () => {
    // const directive = new LoadMoreDirective();
    // expect(directive).toBeTruthy();
  });
});
