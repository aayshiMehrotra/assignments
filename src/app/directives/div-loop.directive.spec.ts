import { DivLoopDirective } from './div-loop.directive';

describe('DivLoopDirective', () => {
  it('should create an instance', () => {
    // const directive = new DivLoopDirective();
    // expect(directive).toBeTruthy();
  });
});
